# #365 Reattach notes
- Named attach via `attach-session -t <name>` supported by MultiplexerState.
- Stale client eviction: new htm replaces old bridge; daemon sends recovery notifications.
- Document that only one active UI client is supported per daemon.
